add_executable(producer Producer.cpp)
add_executable(pullConsumer PullConsumer.cpp)
if (${CMAKE_CXX_COMPILER_ID} MATCHES "Clang")
    target_link_libraries(producer oms boost_thread-mt boost_filesystem-mt boost_chrono-mt)
    target_link_libraries(pullConsumer oms boost_thread-mt boost_filesystem-mt boost_chrono-mt)
else()
    target_link_libraries(producer oms boost_thread boost_filesystem boost_chrono)
    target_link_libraries(pullConsumer oms boost_thread boost_filesystem boost_chrono)
endif()