add_executable(producer Producer.cpp)
add_executable(pullConsumer PullConsumer.cpp)
add_executable(pushConsumer PushConsumer.cpp)
if (${CMAKE_CXX_COMPILER_ID} MATCHES "Clang")
    target_link_libraries(producer dl boost_thread-mt boost_filesystem-mt boost_chrono-mt)
    target_link_libraries(pullConsumer dl boost_thread-mt boost_filesystem-mt boost_chrono-mt)
    target_link_libraries(pushConsumer dl boost_thread-mt boost_filesystem-mt boost_chrono-mt)
else()
    target_link_libraries(producer dl boost_thread boost_filesystem boost_chrono boost_system)
    target_link_libraries(pullConsumer dl boost_thread boost_filesystem boost_chrono boost_system)
    target_link_libraries(pushConsumer dl boost_thread boost_filesystem boost_chrono boost_system)
endif()